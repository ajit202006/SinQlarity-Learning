// Unary operator -> Work on single operand
let x=5;

x++;// post increment -- first uses x then add one 
++x;// pre increment -- first add one in x then uses x
x--;// post decrement -- first uses x then subtracts one from x
--x;// pre decrement -- first subtracts one from x then uses it 

console.log(x);